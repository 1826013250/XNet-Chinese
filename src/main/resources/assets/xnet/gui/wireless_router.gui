window {
    size(168,20)

    bind('public','public')

    panel('','vertical') {
        bgthickness(2)
        togglebutton('public','使其公开') {
            channel('public')
            desired(160,16)
            tooltips('如果选择了这个','那么这个网络将会被公开')
            check(true)
        }
    }
}
